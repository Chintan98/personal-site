---
const links = [
  { text: "Home", href: "/" },
  { text: "Projects", href: "/projects" },
  { text: "About", href: "/about" },
  { text: "Contact", href: "/contact" },
];
---

<nav class="py-8 px-4 flex justify-between items-center max-w-4xl mx-auto">
  <a href="/" class="text-xl font-serif font-bold tracking-tight">Your Name</a>
  
  <ul class="flex gap-6 text-sm font-medium text-gray-600">
    {links.map(link => (
      <li>
        <a href={link.href} class="hover:text-black transition-colors">
          {link.text}
        </a>
      </li>
    ))}
  </ul>
</nav>
